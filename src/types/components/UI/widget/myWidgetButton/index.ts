import {Color, IconName, IconSize, IconVariant} from "types/theme";

export interface MyWidgetButtonProps {
	backgroundColor?: Color;
	iconName: IconName;
	iconColor?: Color;
	iconVariant: IconVariant;
	iconSize?: IconSize;
}

/**
 * Enumeration of the all available icon names.
 * @readonly
 * @enum {string}
 */
export enum IconNames {
	Email = "email",
	Lock = "lock",
	User = "user"
}

export enum IconVariants {
	Outline = "outline",
	Solid = "solid"
}
